input ChargeInput {
  token: String
  "Cantidad a pagar de forma String, para convertilo a entero cero decimal"
  amount: String!
  "Resumen del pedido"
  description: String!
  "Cliente (ID)"
  customer: ID!
  "Divisa"
  currency: CurrencyEnum = EUR
}
